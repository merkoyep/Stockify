query ProductVariantsRenderBefore {
  product {
    id
    title
    variants(first: 100) {
      nodes{
        id
        title
        selectedOptions{
          name
          value
        }
      }
    }
    options {
      id
      name
      values
    }
  }
  shop {
    id
  }
}
